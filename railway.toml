[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Never auto-restart — a FloodWait crash loop makes the ban much worse.
# Railway will still restart on deploy; this only affects crash restarts.
restartPolicyType = "never"

[environments.production]
healthcheckPath = "/health"
healthcheckTimeout = 30
