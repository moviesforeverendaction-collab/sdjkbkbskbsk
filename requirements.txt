# Kurigram — active Pyrogram fork with proper stream_media() offset/limit support
# Drop-in replacement: same imports, same API, just works
kurigram==2.2.19

# C extension — makes MTProto encryption ~4x faster
TgCrypto==1.2.5

# Async MongoDB driver (pinned together — motor 3.3.2 needs pymongo 4.6.x)
pymongo==4.6.3
motor==3.3.2

# Web server
aiohttp==3.9.5

# Fast event loop (Linux/Railway)
uvloop==0.19.0

# Env vars
python-dotenv==1.0.1

# Redis cache (optional)
redis==5.0.4
